import 'package:flutter/material.dart';

class MovieSeats extends StatelessWidget {
  const MovieSeats({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
