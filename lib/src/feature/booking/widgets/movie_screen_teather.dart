import 'package:flutter/material.dart';

class MovieScreenTeather extends StatelessWidget {
  const MovieScreenTeather({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
