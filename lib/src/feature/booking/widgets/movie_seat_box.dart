import 'package:flutter/material.dart';

class MovieSeatBox extends StatelessWidget {
  const MovieSeatBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
